import { MongoMemoryServer } from "mongodb-memory-server";

beforeAll(async () => {
  // Nothing for now
});

beforeEach(async () => {
  // Nothing for now
});

afterAll(async () => {
  // Nothing for now
});